---
type: fixes
source_ref: "pr-1004"
captured_at: "2026-05-17T02:03:35+00:00"
pr_number: 1004
title: "PR #1004 fix results (cycle 3)"
ingested: true
---

## Fix Results

- **PR**: #1004 — fix(workflow): PR Ready 後の Projects Status In Review 遷移漏れを多層観測で防御 (#1003)
- **Type**: fix (cycle 3)
- **Fixed at**: 2026-05-17T09:50+09:00

### Fix Patterns (cycle 3)

cycle 3 で 8 件を本 PR で fix、3 件を別 Issue 化 (refactor scope は #1005 / #1006 / #1007)。

1. **canonical 3-site sync 補完 (F-02 HIGH)**: workflow-incident-emit.sh header docstring が case allowlist (line 80) と error message (line 82) と未同期だった。docstring に新規 2 type を追加して同型化。code-quality + error-handling 独立 HIGH 検出 (高信頼度)。

2. **Self-violation cascade 解消 (F-03 HIGH)**: 本 PR が新規追加した watchdog-status-mismatch.sh が他 3-site (post-compact.sh / start.md / start-finalize.md) に厳格適用している `mktemp + 4-signal trap (EXIT/INT/TERM/HUP)` 契約を遵守せず。trap pattern を導入し SIGINT/SIGTERM/SIGHUP 経路の tempfile leak を解消。

3. **{owner}/{repo} retrieval 内製化 (F-01 HIGH)**: start.md Step 1.5 / start-finalize.md Step 0 が GraphQL で `{owner}/{repo}` を消費するが上流 explicit retrieval phase なし。各 sub-shell scope 内で `gh repo view` を実行し orchestrator 状態依存を排除。

4. **stderr root cause attribution 精度向上 (F-09 LOW-MEDIUM)**: start.md Step 1.5 / start-finalize.md Step 0 で gh と jq の stderr を独立 tempfile に分離し pipeline 失敗時の details に `gh_stderr=` と `jq_stderr=` を併記。

5. **watchdog reconcile stderr capture 対称化 (F-08 LOW-MEDIUM)**: watchdog --reconcile の `2>/dev/null` を tempfile capture に変更し MISMATCHES JSON に `reconcile_stderr` 欄を追加。

6. **STATE_ROOT cd failure 可視化 (F-10 LOW-MEDIUM)**: post-compact.sh に `[ ! -d $STATE_ROOT ]` guard を追加し `state_root_inaccessible` incident を別 emit。

7. **journal comment 排除 (F-04 MEDIUM)**: start-finalize.md Step 0 から F-NN cycle 番号 5 箇所を削除。7f53366c で確立した no_journal_comment 原則を本 PR 自身が踏んでいた self-meta drift fractal を解消。

8. **changelog narrative 削除 (F-06 LOW-MEDIUM)**: workflow-incident-detection.md の 7→9 件 changelog narrative を削除し canonical listing のみに簡素化。

### Statistics
- Total findings: 11
- Fixed in cycle 3: 8
- Separate Issues created: 3 (#1005, #1006, #1007)
- Tests: 91/91 pass (post-compact-reconciliation 12 + projects-status-incident-emit 14 + watchdog-status-mismatch 11 + parent-child-sync-static 54)

### 累積対策 PR 累積知見 (32 回目)
PR #1004 cycle 3 で「Self-violation cascade × N-site canonical sync 不全 × stderr root cause attribution × no_journal_comment self-violation」の 4 軸混在 anti-pattern を実測。本 PR 自身が解決対象とする pattern を本 PR 自身が踏む meta-self-inconsistency の 32 回目記録。cycle 1→2→3 の縮小収束 (7→15→11→8 fix) を示しつつ、Self-violation 経路は cycle 3 でも temporal layer 違反として再演する。

