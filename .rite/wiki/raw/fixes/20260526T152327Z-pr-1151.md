---
type: fixes
source_ref: "pr-1151"
captured_at: "2026-05-26T15:23:27+00:00"
pr_number: 1151
title: "PR #1151 fix results"
ingested: false
---

## Fix Results

- **PR**: #1151
- **Type**: fix
- **Fixed at**: 2026-05-27

### Fix Patterns

Issue #1150 (Wiki commands の Phase → ステップ rename) の fix サイクル 1 で 18 findings に対応。発見された pattern:

1. **Out-of-Scope boundary forward-reference drift (M5 級)**: rename PR で callee→caller 方向の cross-reference を **片方向に翻訳した結果**、caller 側の Out-of-Scope ファイル (pr:*, issue:close) が Phase 構造を維持中であるにも関わらず callee 側 prose が「ステップ X.X.W.2」と書き換わり、実在しない見出しを指す broken cross-ref を生成。3 reviewer (prompt-engineer / code-quality / tech-writer) 全員が独立検出した高信頼度 finding。対策: callee 側 prose の caller 参照を caller の実構造に合わせて Phase 表記で維持する (over-translation 防止)。

2. **Asymmetric Fix Transcription anti-pattern の自己違反**: `docs/anti-patterns/cleanup-wiki-ingest-turn-boundary.md` (本 anti-pattern 自身が記述するファイル) の 8 件中 1 件のみ rename 済で残り 7 件未変換。PR description が「同期対象として明示列挙」していたにも関わらず実 diff が 1 ファイル中の 1 site のみ修正していた。教訓: PR description で同期対象として明示宣言されたファイル群は、commit 前に grep で **全 site が完全置換されている** ことを検証する必要がある (file 単位の done check では不十分、site 単位の done check が必要)。

3. **AC verification grep の盲点**: AC-3 strict (`grep "Phase [0-9]+(\\.[0-9]+)?"` = 0 件) は**「Phase + 数字」のパターンのみ**を対象とし、(a) bare `Phase` 単独 prose (`本 Phase` / `各 Phase` / `後続 Phase` / `5 Phase`) (b) 表ヘッダの Phase 列名 (c) 命名規約 prose 内の `Phase 番号` (d) 副詞句 `現 Phase は set -e なし` 等 を捕捉できない。大規模 rename PR では grep pattern を **word boundary** (`Phase\\b`) で再定義する必要がある。

4. **canonical regex の silent coverage loss**: `backlink-format-check.sh:191` の lint regex が literal `Phase` 固定だったため、本 PR で導入された wiki commands の `Downstream reference: lint.md:ステップ 8.3` 形式が lint coverage から silent に外れる regression を発生させた。対策: source-of-truth の改名に対する lint 側の追従検証を rename PR の DoD に含める。

5. **Mixed terminology in naming conventions**: bash function 命名規約 (`_rite_<scope>_<phase>_cleanup` の `<phase>` token) が「ステップ番号の小数点除外連結形式 → `phase22`」という semantic mismatch を含み、LLM が「ステップ 2.2 → step22」と誤読する経路を生む。対策: 規約 prose に「function 名 token は規約確立 history を保持するため `phase` を維持」と明示。

### Statistics
- Total findings: 18
- Fixed: 18
- Reviewer false positive (withdrawn): 2 (wiki-growth-check.sh:37 / backlink-format-check.sh:44 — top-level commands/lint.md の Phase 3.X 参照は Out-of-Scope のため修正不要)

### Identified Anti-Patterns / Patterns
- **rename-pr-callee-caller-over-translation**: callee→caller 参照を片方向に翻訳する経路で broken cross-ref を silent 生成する anti-pattern (Issue #1150 で 4 sites 検出)
- **ac-grep-narrow-pattern**: AC を `Phase [0-9]+` で定義すると bare prose / 表ヘッダ / 命名規約を捕捉できない (本 PR では 13+ 件残った)
- **lint-canonical-regex-silent-coverage-loss**: literal pattern 固定の lint script が source-of-truth 改名に追従しない経路 (Issue #1150 で `backlink-format-check.sh` で実測)

