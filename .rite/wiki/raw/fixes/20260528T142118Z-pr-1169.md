---
type: fixes
source_ref: "pr-1169"
captured_at: "2026-05-28T14:21:18+00:00"
pr_number: 1169
title: "PR #1169 fix results (cycle 2)"
ingested: true
---

## Fix Results

- **PR**: #1169 (cycle 2)
- **Type**: fix

### Fix Patterns
3 件修正 (F-01 HIGH / F-02 MEDIUM / F-03 MEDIUM)。
教訓: **consume 操作 (read + delete + return value) は delete-then-emit が fail-closed の正解**。値を先に出力すると、削除失敗時に「消費したつもりで未消費」状態で消費者 (Stop hook) が値に基づき block を確定し、永続 FS 障害下で無限ループする。同一ファイル内の対称 helper (cmd_set 等) が _atomic_write を `|| return 1` で fail-closed 伝播 + 診断 emit するのに consume-handoff だけ `|| return 0` 無診断という Asymmetric Fix Transcription が根本。修正は (a) reorder で fail-closed 化、(b) 診断 emit で対称化、(c) 呼び出し側 hook の 2>/dev/null を RITE_DEBUG gate 化して診断を surface 可能に。
read-only state dir (chmod 0555) で runtime 再現 → 値 withheld + ERROR emit を確認した検証手法が有効。

### Statistics
- Total findings: 3
- Fixed: 3
- Replied: 0

