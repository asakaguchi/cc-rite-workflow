---
type: fixes
source_ref: "pr-608"
captured_at: "2026-04-19T21:47:15+00:00"
pr_number: 608
title: "PR #608 re-fix cycle 2 results"
ingested: true
ingest_status: skipped
skip_reason: "多箇所統一取り残し / 補足語構造不整合 / prose 競合 fix パターンは asymmetric-fix-transcription / canonical-reference-sample-code-strict-sync で網羅済み (raw: pr-608)"
---

## Fix Results (Re-fix cycle 2)

- **PR**: #608 — fix(pr-cleanup): wiki-auto-ingest 後の Phase 5 停止を多層防御で解消
- **Type**: fix (re-fix cycle 2)
- **Fixed at**: 2026-04-20T06:35:00+09:00

### Fix Patterns (前回 10 findings 修正の副作用修正)

Re-review で検出された 3 findings に対応。いずれも前回 cycle の修正取り残し / 副作用:

1. **prose 多箇所統一の取り残し (HIGH)**: 前回「caller continuation marker 4 箇所統一」で L105/L852/L1023/L1028/L1032 は統一したが、L88 Correct-pattern Step 3 の `(single execution path, caller embedded)` 修飾子を grep 漏れで残した。今回 L88 を `(Phase 9.1 設計判断により実行経路を問わず常に出力)` に書き換え 5 箇所完全統一。

2. **修正時補足語の構造不整合 (MEDIUM)**: 前回 F-07 修正で Defense-in-depth に追加した「(Phase 4 heading 外)」が実ファイル構造 (Phase 4.W の h3 subsection 配下) と食い違っていた。heading 階層を grep で verify せず prose 追加した結果。今回 `(Phase 4.W sub-section: ### 🚨 Mandatory After Wiki Ingest at h3, inside ## Phase 4.W)` に書き換え。

3. **修正副作用による prose 競合 (MEDIUM)**: 前回 F-03 修正 (retrograde transition prose を whitelist 非依存表現に書換え) の結果、Self-check Yes 分岐 (L1609) が Correct-pattern L72 (Step 1 正規路) と表面的に矛盾する表現になった。phase 依存の条件分岐だが前提条件 (cleanup_completed 到達済み) が prose で暗黙化。今回 Yes 分岐冒頭に「terminal 到達後の重複呼び出し防止の例外経路」を明示し、No 分岐にも non-terminal 状態の明示を追加。

### Statistics
- Total findings: 3
- Fixed: 3 (prose 修正のみ、各 1 行レベル)
- Replied: 0
- Skipped: 0

### 経験則 (Re-fix で再確認された原則)

- **Grep verification in multi-site prose unification**: prose 統一タスクは「全該当箇所を grep で機械検証」が必須。目視での「4 箇所統一済み」判定は Correct-pattern のような heading 別 section の drift を取り残す。同一 policy (例: 「常時出力」) を複数箇所に書く際、**最終チェック** として `grep -F '<古い表現>'` と `grep -F '<新しい表現>'` の両方を実行し、前者 0 件 + 後者 N 件を確認する。
- **Heading reference verification**: prose に補足語として heading を参照する際 (例: `(Phase 4 heading 外)`)、必ず `grep '^##\|^###'` で実ファイルの階層構造を確認する。heading 参照系の drift は cross-file integrity check では検出困難で、re-review で露呈する傾向がある。
- **Prose refactor side-effect check**: 既存 prose を書換える際、書換え後の文が**他の section の文と表面矛盾しないか**を clue word で検索 (例: 「正規路」「禁止」「必須」)。条件分岐の暗黙前提 (phase=X のときのみ) を明示しないと、reader が独立に読んだ際に誤解するリスクが高い。

