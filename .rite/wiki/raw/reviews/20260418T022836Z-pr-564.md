---
type: reviews
source_ref: "pr-564"
captured_at: "2026-04-18T02:28:36+00:00"
pr_number: 564
title: "PR #564 review results"
ingested: true
ingest_status: skipped
skip_reason: "cycle 8 review: mktemp failure 対応、既存 mktemp-failure-surface-warning.md でカバー済"
---

## Review Results

- **PR**: #564 — fix(wiki): lint missing を 2 カテゴリに分離し .gitignore で develop 漏洩を防ぐ
- **Type**: review
- **Reviewed at**: 2026-04-18T11:24:07+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling

### Finding Patterns

本レビューで検出された PR 固有の経験則パターン:

1. **intra-PR regulation self-drift**: 同一 PR 内で「行番号参照 drift 廃止」規約 (cee37ca) を establish した後、最終 cycle で新規コメントにより規約自身を違反する drift が発生 (F-01)。規約 establishment 後の残作業で regulator が respect されず silent drift する経路が実証された。対策: 規約 establish 時に「以降 commit は grep/lint で automatic 検出」を同時に設置する。

2. **cross-path relative import drift**: 同一 reference file への相対パスが、同一 source file 内の複数 site で統一書式にされておらず、新規 site 追加時に 1 箇所だけ drift する (F-02: `references/bash-trap-patterns.md` vs `../pr/references/bash-trap-patterns.md`)。対策: relative path を symbolic anchor (Phase + semantic) に置換、絶対パス化、または単一 vocabulary に集約。

3. **enumeration site count drift**: 同型参照コメントで「N 箇所」と書かれる site 列挙が、対象 site 数が増えた際に更新されず drift する (F-03: `branch_strategy` fail-fast 2→5)。対策: 「N 箇所」ではなく具体的 Phase 列挙 (`Phase 2.2/6.0/6.2/8.2/8.3`) で記述し、Grep で detect 可能にする。

4. **warning template file scope drift**: エラー種別ごとの warning template で、指示する対象ファイルが実際の障害対象と一致しない (F-04: log.md 読出失敗なのに `.rite/wiki/pages/` 確認を勧める)。対策: enum 値ごとに warning template を完全分離し、対象ファイルを canonical に記述。

5. **naming convention self-matching failure**: 命名規約セクションが「参照実装」として既存命名を例示しつつ、新規 rule では異なる命名に切り替える内部矛盾 (F-05: `phase2` vs `phase22`)。対策: 既存命名互換性 + 新規命名規約の両立を明示記述 or 全面 rename。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 3
- LOW: 1

