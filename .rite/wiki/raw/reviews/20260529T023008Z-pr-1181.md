---
type: reviews
source_ref: "pr-1181"
captured_at: "2026-05-29T02:30:08+00:00"
pr_number: 1181
title: "PR #1181 review results"
ingested: false
---

## Review Results

- **PR**: #1181 — refactor(hooks): flow-state.sh の jq stderr スニペットに control-char 中和を追加
- **Type**: review
- **Reviewed at**: 2026-05-29T11:29:21+09:00
- **Reviewers**: security, code-quality, error-handling, test

### Finding Patterns
4 名のレビュアー全員が「可 / 指摘事項 0 件」で合意。本 PR は flow-state.sh の 4 つの jq stderr 診断スニペット emission site (cmd_set / cmd_get×2 / cmd_consume_handoff) を共通 helper `_emit_jq_err_snippet()` に集約し control-char (`[[:cntrl:]]`→`?`) 中和を加える防御的 refactor。

経験則として有用なパターン:
- **Asymmetric Fix Transcription の構造的回避**: 同一イディオムが散在する 4 site を単一 helper に集約することで、中和ロジックを 1 経路に集中させ「対称位置への伝播漏れ」を構造的に防いだ。テスト (TC-23) も helper 1 経路を pin すれば全 site をカバーできる設計。レビュアー全員がこの設計を高評価。
- **exit-code 等価性検証**: 旧短絡 AND 連鎖 (`[ -n ] && [ -s ] && head|sed`、空ファイルで rc=1) → 新 helper (`if ...; then head|sed || true; fi`、no-op rc=0) の置換で、4 call site すべてで helper の rc が後続から参照されないこと、`set -e` が if/&& condition 位置で abort しないことを実機検証し挙動等価を確認。
- **scope 規律の好例**: sibling hooks 約 60-80 箇所に同型の未中和イディオムが残存するが、revert test fail (pre-existing) かつ Issue #1173 のスコープ (flow-state.sh 限定) 外として全レビュアーが指摘ではなく調査推奨/boundary 推奨に正しく分類。CLAUDE.md「スコープを越えない」原則と整合。
- **中和テストの revert 耐性**: TC-23.2 (生 ESC 不在) + TC-23.3 (`?` への 1:1 置換) の二重 assertion が、空削除 revert (`s///g`) と snippet 全 drop の両 mutation を catch する設計 (test reviewer が worktree で 3 mutation 実機検証)。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW: 0

