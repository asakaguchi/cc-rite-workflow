---
type: reviews
source_ref: "pr-1139"
captured_at: "2026-05-25T07:07:27+00:00"
pr_number: 1139
title: "PR #1139 review results"
ingested: true
---

## Review Results

- **PR**: #1139 — docs: v0.5.0 リリース前の公式ドキュメント見直し
- **Type**: review
- **Reviewed at**: 2026-05-25T16:06:51+09:00
- **Reviewers**: tech-writer-reviewer (Doc-Heavy mandatory), code-quality-reviewer (co-reviewer, added via sole-reviewer guard)

### Finding Patterns

5 findings detected across 4 categories of doc drift:

1. **Anchor link typo introduced by section refactor** (CRITICAL): GitHub slug algorithm produces `riteissuestart-retired-in-1136` from `### /rite:issue:start (Retired in #1136)`, but the PR wrote `riteissuesart-retired-in-1136` (missing `t`). Both SPEC.md and SPEC.ja.md affected (mirror typo).

2. **Phantom sentinel in Terminal marker table** (HIGH): Added `[iterate:completed]` and `Workflow Termination block` references to docs/SPEC.md Terminal marker table for the newly-decomposed 4 commands (#1136). Neither exists in implementation: iterate.md emits `[review:mergeable]` / `[fix:replied-only]` / `[fix:cancelled-by-user]`, and ready.md outputs only `[ready:completed]`. The PR repurposed terminology from the retired `/rite:issue:start` orchestrator.

3. **Asymmetric Fix Transcription on #1118 wave** (HIGH/MEDIUM): The PR DEPRECATED-ified `separate_issue_creation.*` keys (one of three keys removed in #1118), but missed the other two: `observed_likelihood_gate.*` / `fail_fast_first.*` (HIGH) and `fix.severity_gating` (MEDIUM). The same PR's CHANGELOG.md L36 explicitly mentions #1118 removed all three, but CONFIGURATION.md / CONFIGURATION.ja.md still document them as live keys.

### Lessons (anti-patterns reinforced)

- **Asymmetric Fix Transcription (re-confirmed for v0.5.0 docs PR)**: When applying a DEPRECATED pattern to one key from a removal wave, grep ALL keys mentioned in the wave's CHANGELOG entry and apply the same pattern uniformly. The reviewer caught this by cross-referencing the PR's own CHANGELOG against unmodified CONFIGURATION lines.

- **Mass refactor anchor-link integrity check**: When a heading is added/renamed in a way that should generate a Markdown anchor (e.g., `### /rite:issue:start (Retired in #1136)`), verify the generated slug by GitHub's slug algorithm (lowercase, remove punctuation except hyphens, replace spaces with hyphens). Typos are silent until a reader clicks the link.

- **Terminal marker table verification protocol**: When updating sentinel/marker tables for a multi-command decomposition (#1136-class), verify each row by grep-ing the new sentinel literal in the corresponding command file (`grep -n '\[iterate:completed\]' plugins/rite/commands/pr/iterate.md` etc.). Repurposing terminology from a retired orchestrator without checking the new orchestrator's actual output is a recurring failure mode.

### Severity Distribution
- CRITICAL: 1
- HIGH: 3
- MEDIUM: 1
- LOW: 0

