---
type: fixes
source_ref: "pr-1148-cycle-2"
captured_at: "2026-05-26T06:43:13+00:00"
pr_number: 1148
title: "PR #1148 cycle 2 fix results"
ingested: true
---

## Fix Results

- **PR**: #1148 cycle 2
- **Type**: fix
- **Fixed at**: 2026-05-26T15:50:00+09:00

### Fix Patterns
- **Review-context-comment-leak 一括除去**: cycle 1 fix で混入した journal-style コメント (F-NN 対応 / 旧実装は L158 の / 旧 L455 + Issue #1019 で修正) を 4 箇所すべて除去。`no_journal_comment` + `no_line_or_cycle_reference` 原則違反は WHY のみ残す方針で対応。修正経緯は commit message と PR description に集約する原則を遵守。
- **Asymmetric Fix Transcription の対称化 (N+1 回目)**: cycle 1 で F-09 fix を `cmd_get` の 3 箇所に適用したが `cmd_set` の同型 WARNING 2 箇所に未適用だった drift を対称化。「`cmd_get` で basename 化したら `cmd_set` でも basename 化する」が SoT 移譲の前提。Wiki anti-pattern: 同関数族内の対称性チェック義務。
- **Dead code removal (RETURN trap 導入後の inline rm)**: cycle 1 で RETURN trap を追加した結果 `cmd_set` L197 の inline rm が dead-effect duplication 化していた。trap 追加と inline rm 削除を**ペアで実行**しない fix-introduced pattern。
- **Bash builtin shadow 回避**: 引数名 `source` (= `.` command) を `origin` に rename。同 file の他関数と naming convention 統一。subtle で diagnostic 時の混乱源になりやすい naming は production code では避ける。
- **Negative assertion による partial regression guard**: TC-19.4 で「rejection 発生」だけでなく「injection 内容が stderr に漏出していない」を negative-assert。validator が `[[:cntrl:]]` を `\n` のみに狭めた partial regression に対する直接 guard。chokepoint を信頼するだけでなく invariant そのものを pin する。
- **Test coverage 拡張 (chokepoint 単一依存からの脱却)**: cycle 1 で `_validate_session_id` を 4 経路に適用したが TC は env-var 2 経路のみ pin。TC-18.3/18.4 で override + SESSION_ID_FILE 経路を追加し、将来 validator の call-site が分岐された場合の silent regression を catch。precedence (TC-13.4) も同様に未 pin だった暗黙 contract を明示化。
- **Cycle 1 explicit decision の respect**: cycle 2 code-quality reviewer が「UUID validator 統合を HIGH × current-pr に昇格」と要求したが、cycle 1 commit message で「follow-up Issue で扱う方が scope creep を防げる」と決定済。recommendation 経路に降格して cycle 1 decision を尊重 (review-fix loop で前 cycle の design decision を後 cycle が override する pattern の防止)。

### Statistics
- Total findings: 12
- Fixed (code): 11 (current-pr) + 1 (nit-noted, F-12 source→origin embedded in same commit)
- Replied: 0
- New test sub-tests: 5 (TC-13.4 precedence + TC-18.3/18.4 override/SESSION_ID_FILE + TC-19.3 CR + TC-19.4 negative assertion)
- Test count: 58 → 63 (+5)

