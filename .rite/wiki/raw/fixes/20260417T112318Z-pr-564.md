---
type: fixes
source_ref: "pr-564"
captured_at: "2026-04-17T11:23:18+00:00"
pr_number: 564
title: "PR #564 fix results"
ingested: true
ingest_status: skipped
skip_reason: "cycle 1 fix: 初期実装、新規経験則は抽出済みの 2 ページと identity #6 に統合"
---

## Fix Results

- **PR**: #564
- **Type**: fix
- **Fixed at**: 2026-04-17

### Fix Patterns

**CRITICAL (LLM↔bash variable boundary)**: bash 内で構築した集合を LLM が後続 Phase で参照する設計のとき、`echo "count=N"` だけでは集合本体が LLM に届かない。bash 変数は Bash tool 呼び出し境界を超えると失われるため、**delimiter 付きで stdout に集合本体を出力する必要がある** (例: `---skipped_refs_begin---` / `---skipped_refs_end---`)。0 件でも delimiter は出力し、構築ステップ実行の positive confirmation とする。

**HIGH (selective surface pattern の二重適用)**: 同一ファイル内で Phase A が stderr 退避 tempfile + head -3 pattern を採用しているのに Phase B が `2>/dev/null || true` で全廃棄する非対称設計は、legitimate absence と真の IO error を silent 同視する regression の温床。全 phase で stderr を tempfile に退避し、stderr 内容の pattern matching で 2 ケースを区別する selective surface pattern を統一する。

**HIGH (pipefail 必須 + awk/sort exit code)**: bash の `grep -c . || true` は exit 2 (IO error) も silent 吸収する。`awk 'NF>0 {n++}'` で件数カウントし、pipefail enable + 独立 tempfile で awk/sort stderr を捕捉する pattern に統一する。

**HIGH (scope drift 同期)**: frontmatter description / intro 文言 / 観点表 / SKILL.md / ingest.md 導入文の 5 箇所で同一概念の記述が drift すると、ユーザーの認知モデルとコード実体が乖離する。本体 Phase の仕様変更時は必ず周辺記述 (Step 6c) と SKILL.md / 他コマンドとの cross-file impact (Step 8) も同期する。

### Statistics
- Total findings: 18
- Fixed: 16 (CRITICAL 1 + HIGH 7 + MEDIUM 5 + LOW 3)
- Replied: 0
- Skipped: 0
- Deferred: 2 (F-10 は selective surface 実装に内包、F-18 は audit trail 新機能で scope 外)

