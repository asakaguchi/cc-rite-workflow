---
type: reviews
source_ref: "pr-1164"
captured_at: "2026-05-27T16:39:52+00:00"
pr_number: 1164
title: "PR #1164 review results"
ingested: true
---

## Review Results

- **PR**: #1164 — docs(workflow): SKILL.md に multi-step skill での TaskCreate 利用ルールを追加
- **Type**: review
- **Reviewed at**: 2026-05-28T01:38:29+09:00
- **Reviewers**: prompt-engineer, code-quality

### Finding Patterns

本 review で観測された経験則的に有用な pattern:

1. **Fact-check Critical**: 「ツール / 識別子が存在しない」型の指摘 (code-quality F-X1 で CONTRADICTED) は、本会話の system reminder の deferred tools registry を直接 grep / 観測することで empirical 検証可能。reviewer が grep を codebase 単一観測点に限定すると、Claude Code native tool (TaskCreate / TaskUpdate / TaskList / TodoWrite 等) の registry 外実在を見落とす silent regression が起きる。fact-check phase で「session deferred-tools registry 一覧 + 会話内実呼出履歴」を観測点に追加すると false-positive を排除できる。

2. **Prompt rule の action verb 精度**: 「〜してから〜する」型のガード文言は LLM が「最初のステップ完了 → 次のステップへ無条件遷移」と解釈する経路があり、ガード条件 (「X が成立する場合のみ Y」) を action verb で明示する必要がある。本 PR で観測された MEDIUM finding #1 (F-01) はこの pattern。

3. **Nested skill responsibility 境界の明示要件**: skill call chain (cleanup → wiki:ingest → wiki:lint) において「return 時点」の指す point が中間か最外側か曖昧な場合、LLM の解釈が分散する。TaskCreate を発行した skill = 最外側 という invariant を明示的に書くか、全 return point で重複 check を許容するかを明示する必要がある (F-02)。

4. **Identifier prefix consistency**: 同一行内・同一 paragraph 内で同一 entity を 2 つの異なる表記 (例: `pr:cleanup` vs `cleanup`) で参照すると、LLM の cross-file impact check (Grep 等) で hit miss を生む (F-03)。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0 (1 件 CONTRADICTED で除外)
- MEDIUM: 3
- LOW-MEDIUM: 1 (nit-noted)
- LOW: 0

