---
type: reviews
source_ref: "pr-1148"
captured_at: "2026-05-26T06:05:27+00:00"
pr_number: 1148
title: "PR #1148 review results"
ingested: true
---

## Review Results

- **PR**: #1148 — fix(hooks): flow-state.sh の get / set --if-exists の silent failure を解消
- **Type**: review
- **Reviewed at**: 2026-05-26T15:04:43+09:00
- **Reviewers**: error-handling, code-quality, test, security

### Finding Patterns
- **Asymmetric Fix Transcription (再々発)**: cmd_get の --jq-filter 経路には明示的 return 0 があるが --field 経路には無く、関数末尾の `[ -n "$jq_err" ] && rm -f` で mktemp 失敗時に rc=1 を漏らす silent failure を新規導入。Issue #1142 が潰そうとした「rc=0 + 空」と逆方向の「rc=1 + 正しい stdout」を作る、本 PR の目的に反するパターン。「対称位置への伝播漏れ」累積パターンとして再発を実測。
- **Env var resolution の trust model drift**: 既存 5 helper は _resolve-session-id.sh (RFC 4122 strict) を経由しているが、flow-state.sh の env-var 経路だけ無検証で sid を返す。本 PR で `CLAUDE_CODE_SESSION_ID` 受け入れを追加した際、validation の copy も同時に必要だったが skip された。canonical helper 経路を経由する DRY pattern からの drift。
- **WARNING 出力の log injection vector**: 新規追加した WARNING に attacker-controlled な $sid を unescape で埋め込んでおり、改行注入で偽の WARNING 行を stderr に injectable。observability + regression test が攻撃者制御 log で false-pass する経路を作る。silent failure 解消 PR が新たな silent failure 経路を作るパターン。
- **Negative test comment self-contradiction**: TC-17.2 の Why コメントが「rc=0 graceful contract」と「rc=1 + ERROR」の両方を書く自己撞着。Comment Rot の手前のレビュー時点での内的矛盾、コードレビュー時の WHY コメント整合性 check の重要性。
- **Test brittleness — hardcoded WARNING string match**: grep -q による前方一致検出が test SoT を file:line で分散させており、WARNING 文言変更で test brittle。文言 ERE pattern 化 or 共通定数化が必要。

### Severity Distribution
- CRITICAL: 1
- HIGH: 2
- MEDIUM: 2
- LOW: 4

