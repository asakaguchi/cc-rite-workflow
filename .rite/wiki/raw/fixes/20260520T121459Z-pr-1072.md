---
type: fixes
source_ref: "pr-1072"
captured_at: "2026-05-20T12:14:59+00:00"
pr_number: 1072
title: "PR #1072 fix results"
ingested: true
---

## Fix Results

- **PR**: #1072
- **Type**: fix
- **Fixed at**: 2026-05-20

### Fix Patterns

本 fix は「累積対策 PR の review-fix loop 内 fix 自体が drift を導入する」fractal pattern (Wiki 経験則) の予防に注目。Layer 4 hook 追加に伴う以下 3 種の silent failure 経路を修正:

1. **silent suppression に retained flag を追加**: `jq -r ... 2>/dev/null || exit 0` パターンを `if jq ...; else rc=$?; echo "[CONTEXT] *_FAILED=1"; exit 0; fi` に置換することで、Layer 4 hook が「動かない」ケースも後追い可能にする。Wiki 経験則「`no_unnecessary_fallback` violation」「silent failure hunter」の典型実践。

2. **命名規約の対称化**: 既存 6 hooks (`post-tool-wm-sync.sh` / `pre-tool-bash-guard.sh` / `session-start.sh` 等) の `_RITE_HOOK_RUNNING_<NAME>` パターンに合わせて recursion guard 変数を rename。「累積対策時に既存パターンとの非対称が drift を生む」の予防。

3. **prose drift 防止**: 機構を追加した時、同一ファイル内で当該機構を参照する prose も同期更新する。Wiki 経験則「N-site canonical sync」の系統で、本 fix では "two/three active layers" の言及を 2 site で同期。片側だけ更新で逆の経路 (silent contradiction) を作る anti-pattern を防ぐ。

### Statistics
- Total findings: 8 (current-pr 6 + follow-up 2)
- Fixed: 6 (current-pr scope)
- Replied: 0
- Skipped → 別 Issue: 2 (follow-up scope, #1073 / #1074)

### Wiki 経験則の利用と発火傾向

レビュー時に注入された Wiki 経験則 (`累積対策 PR は fractal pattern` / `declarative enforcement limit` / `Success-only Sentinel Design`) はすべて本 fix の判断材料として有効に機能した。特に「declarative limit」は Layer 4 hook 自体の rationale でもあるため、reviewer の指摘 (silent suppression 経路の追加) を critical に受け止める論理的根拠となった。

