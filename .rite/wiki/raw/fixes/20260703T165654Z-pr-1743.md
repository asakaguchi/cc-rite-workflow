---
type: fixes
source_ref: "pr-1743"
captured_at: "2026-07-03T16:56:54+00:00"
pr_number: 1743
title: "PR #1743 fix results"
ingested: true
---

## Fix Results

- **PR**: #1743
- **Type**: fix
- **Fixed at**: 2026-07-04T02:05:00+09:00

### Fix Patterns
- 検証スクリプト自身の失敗モード（位置依存パースの表形式変更耐性）には、集合抽出ガードと対称の「検査行数ガード」を付けると silent false pass を構造的に遮断できる
- `--flag) VAR="$2"; shift 2` は値欠落時に set -u 経由で誤った exit code を返す。exit code 契約を守るには消費前の `[ $# -ge 2 ]` 検証が必須（sibling スクリプトにも同パターンが pre-existing — 別 Issue 対応）
- テスト fixture の変異設計では「どの不変量が発火するか」を実行で確認する。一方向差し替えは集合系不変量も同時発火するため、行内整合チェックの分離検証には均衡入替（双方向 swap）を使う
- 双方向チェックの reverse 方向は明示 TC が無いと未 pin（削除しても green）。「存在しない subagent を spawn する」側の failure mode を独立に pin する
- テストの in-place 編集は sed -i でなく awk read→transform→write→mv（BSD sed 互換のリポ規約）

### Statistics
- Total findings: 6
- Fixed: 6
- Replied: 0

