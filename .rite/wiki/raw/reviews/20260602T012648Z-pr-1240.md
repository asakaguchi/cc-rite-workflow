---
type: reviews
source_ref: "pr-1240"
captured_at: "2026-06-02T01:26:48+00:00"
pr_number: 1240
title: "PR #1240 review results"
ingested: false
---

## Review Results

- **PR**: #1240 — refactor(hooks): RITE_HOOK_RE を .py 単一定義化し正規表現重複を解消
- **Type**: review
- **Reviewers**: prompt-engineer, code-quality, error-handling, security

### Finding Patterns

inline python3 → 共有 .py 委譲 refactor のレビュー。3 reviewer (prompt-engineer/code-quality/security) は「可」、code-quality は委譲の挙動等価性 (stdin→stdout I/O 契約・exit code 0/1/2・regex・json.dump フォーマット) を 45 契約テスト + 4 ケース実測で確認。

**最重要の経験則 — reviewer-attributed regression の revert test 検証**:
error-handling reviewer が CRITICAL ×2 を指摘（「set -e 下で python3 単独文が rc≠0 で hook abort、エラー報告分岐が dead code 化」）。現象自体は経験的に再現でき実在する。しかし reviewer は「旧コードは `if [ -n "$_repair_tmp" ] && python3 -c` の && 形式で set -e 免除されていた」と主張し、これを根拠に「本 PR が導入した regression」と framing した。

orchestrator が `git show develop:session-start.sh` で検証した結果、**旧コードも python3 を単独文として置いており（&& なし）、構造が新旧で完全同一**であることが判明。revert test 不成立 = pre-existing。reviewer は旧コード構造を fabricate していた（"git show main: で確認" と書いたが実際は異なる）。

→ **教訓**: reviewer が「revert で消える regression」と主張しても、orchestrator は revert test を独立に git show / git diff で検証すべき。特に CRITICAL かつ他 reviewer と矛盾する場合。reviewer の repro が現象の実在を示しても、それが PR 由来か pre-existing かの attribution は別途検証が必要。Asymmetric Fix Transcription (inline→delegate refactor で guard が落ちる) は実際の failure mode だが、本件では guard は新旧とも同形で「落ちていない」（pre-existing な構造的問題）。

pre-existing と判明した潜在バグは rite scope ルール (revert test) により blocking finding にせず別 Issue 化 (#1241)。ユーザー escalation で「マージ可＋別Issue化」を選択。

### Severity Distribution
- CRITICAL: 0 (error-handling の CRITICAL 2 件は revert test 不成立で pre-existing と判明し別 Issue #1241 化)
- HIGH: 0
- MEDIUM: 0
- LOW: 0

