---
type: fixes
source_ref: "pr-1306"
captured_at: "2026-06-08T12:10:39+00:00"
pr_number: 1306
title: "PR #1306 fix results (cycle 2)"
ingested: true
---

## Fix Results

- **PR**: #1306
- **Type**: fix (cycle 2)

### Fix Patterns
ユーザーが Step 7 で follow-up 推奨を「本 PR で対応」に選択した結果の test 強化 cycle:
- F-01: 静的 test の T-4 を「非ゼロ」判定から「exit 2 明示」assert に変更し bare `--limit` 値欠落ケースを追加。exit-code 契約 (exit 1=drift warning / exit 2=invocation error) を持つスクリプトの test は「非ゼロ」でなく「正確な exit code」を assert すべき (F-01 type の regression を捕捉)。capture line は set +e/set -e で囲み set -euo pipefail 下の harness abort を回避。
- F-02/F-03: 静的 source grep (`COMPLETED` / `"Done"`) が header comment にマッチし「文字列の存在」を検証していた弱点を、quoted jq 述語 (`stateReason == "COMPLETED"` / `select($st != "Done")`) への anchor で「検出ロジックの存在」検証に強化。mutation (jq 述語削除) で assert fail を確認。教訓: 静的 test の grep は被テストロジック行に anchor すべき (header comment と区別できる quoted/predicate 形を選ぶ)。quoted COMPLETED 述語は AC-2 NOT_PLANNED 除外も pin する (誤形化で literal が消える)。

### Statistics
- Total findings: 3
- Fixed: 3
- Replied: 0

