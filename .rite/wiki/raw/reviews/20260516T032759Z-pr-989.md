---
type: reviews
source_ref: "pr-989"
captured_at: "2026-05-16T03:27:59+00:00"
pr_number: 989
title: "PR #989 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #989 — test(hooks): subdirectory invocation 回帰テスト追加 (TC-10 / TC-016)
- **Type**: review (cycle 2, re-review after fix)
- **Reviewed at**: 2026-05-16T12:27:20+09:00
- **Reviewers**: test-reviewer, code-quality-reviewer

### Finding Patterns
Cycle 2 では cycle 1 で検出された F-01 (TC-10 inline jq vs build_stop_payload helper 不統一, MEDIUM) と F-02 (TC-016 `( ... ) || true` で setup error mask, LOW cross-validated) の修正検証。両 reviewer から「修正は cycle 1 指摘の趣旨どおりに行われており、追加調査の必要はない」「mutation kill capability 実証 (STATE_ROOT_PATH="$CWD" mutation で両 TC が FAIL)」と評価され blocking finding なし。3-axis pin (exit 2 + ACTION + no sentinel) による Test pin protection theater 回避が両 reviewer から spec 整合として確認された。

経験則として捕捉価値あるパターン:
1. **Test helper symmetry**: 新規 TC 追加時に sibling TC (TC-1〜TC-9) の helper pattern を踏襲することで cross-validated review で同種指摘 (重複排除/helper 統一) を未然防止できる
2. **Setup error vs test failure 区別**: `( ... ) || true` パターンは setup 失敗 (mktemp / git init) と walkup 不能化 (本来の test 対象) を syndrome 上 indistinguishable にする false-negative 入口、`if ! ( ... ); then ERROR + skip; else ... fi` の fail-fast 構造で区別可能化
3. **Test pin protection theater 回避**: Issue 原文の literal example (例: 単一 `grep -q sentinel` assertion) を踏襲すると gate 不発時の vacuously pass を生む。flow-state setup + multi-axis pin (exit code / message / sentinel) の 3-axis 構造で discriminating power を確保
4. **Mutation testing 真正性検証**: state-path-resolve.sh の git rev-parse 一時無効化 (`STATE_ROOT_PATH="$CWD"`) で両 TC の FAIL を empirical 確認 → restore は計画通り; mutation 中 file の back-up は `state-path-resolve.sh.bak` 不要 (git stash で十分)

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW: 0

### Recommendations (non-blocking, 3 件)
- Comment 精度 (notification.test.sh:303): "make_sandbox と同じ fail-fast 構造" → 厳密には exit 1 vs skip の差異あり (本 PR scope 内、軽微)
- Helper 集約 (`_test-helpers.sh`): make_sandbox 相当 git-init sandbox が 5+ 箇所重複 (別 Issue 候補)
- Helper module: notification.test.sh 側にも make_sandbox helper を導入し sibling symmetry を完全化 (将来 Issue 候補)

