---
type: reviews
source_ref: "pr-564"
captured_at: "2026-04-17T11:15:31+00:00"
pr_number: 564
title: "PR #564 review results"
ingested: true
ingest_status: skipped
skip_reason: "cycle 1 review: 初回指摘 (CRITICAL 3 / HIGH 5 / MEDIUM x) は既存ページ (drift-check-anchor-prose-code-sync / prose-design-without-backing-implementation) でカバー済"
---

## Review Results

- **PR**: #564 — fix(wiki): lint missing を 2 カテゴリに分離し .gitignore で develop 漏洩を防ぐ
- **Type**: review
- **Reviewed at**: 2026-04-17T20:13:26+09:00
- **Reviewers**: prompt-engineer, code-quality, error-handling

### Finding Patterns

**CRITICAL cross-validated pattern**: bash block が LLM 向け集合を stdout に明示出力しない設計欠陥。Phase 6.0 で `skipped_refs` 集合を構築しているが `printf '%s\n' "$skipped_refs"` ではなく `echo "skipped_refs_count=$N"` のみ echo している。Bash ツール呼び出し境界を超えるとシェル変数は失われるため、後続 Phase で LLM が集合本体を参照できず機能破綻。prompt-engineer と code-quality の 2 reviewer が独立に CRITICAL 指摘 (cross-validated)。

**HIGH silent regression pattern**: `2>/dev/null || true` で log.md read 失敗を silent 吸収し legitimate absence (fresh branch) と IO error を区別不能。同一ファイル内の Phase 2.3 は selective surface pattern (stderr 退避 tempfile + head -3) を適用しており非対称。code-quality と error-handling の 2 reviewer が独立に指摘。

**HIGH scope drift pattern**: lint.md frontmatter description / intro "5 観点" / 観点表 / ingest.md 導入文 / SKILL.md の 5 箇所で旧 5 フィールド記述が残存。body の Phase 6-9 は 6 フィールド化したが周辺記述の同期を忘れる drift。

### Severity Distribution
- CRITICAL: 1
- HIGH: 7
- MEDIUM: 6
- LOW: 4

