---
type: reviews
source_ref: "pr-564"
captured_at: "2026-04-17T15:26:50+00:00"
pr_number: 564
title: "PR #564 review results"
ingested: true
ingest_status: skipped
skip_reason: "cycle 5 review: placeholder gate 修正、既存 bash-if-bang-rc-capture.md でカバー済"
---

## Review Results

- **PR**: #564 — fix(wiki): lint missing を 2 カテゴリに分離し .gitignore で develop 漏洩を防ぐ
- **Type**: review
- **Reviewed at**: 2026-04-18T00:25:17+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling

### Finding Patterns

1. **Placeholder-without-source** — Phase 9.1 template に `{n_pages}` / `{n_raw}` が登場するが counter table にも emit 経路にも存在せず、LLM が literal placeholder を出力する silent regression 源になる。review の中で最重大な structural drift
2. **Cross-phase 契約非対称** — Phase 2.2 / 6.0 / 8.2 の fail-fast メッセージで「同型化」を宣言しながら 1/3 未適用、Phase 8.3 step 1/3/4 の `n_warnings += 1` で step 4 のみ抜け、Phase 6.2 の trap 欠落など、新規 phase 追加時に既存パターンと対称化する規律が弱い
3. **Legacy 表記残留** — 仕様 shift に伴い ingest.md の冒頭 spec「stdout 空 = 0 件」が残存し、step 3 の新契約「stdout 空 = 失敗」と矛盾。将来の maintainer が top-down reading で legacy 動作を復元する risk path
4. **ハードコード行番号 drift** — `.gitignore` コメントが lint.md L869 を参照するが実際は L963 (94 行 drift)。同 PR 内で発生しているため同種のフラジリティが他所にもある可能性
5. **enum 宣言 vs 実装不一致** — `all_source_refs_read_ok` は 4 値と宣言するが 3 値しか set せず `partial_error` は dead value。Phase 9.1 template 側にも対応 placeholder が無い

### Severity Distribution
- CRITICAL: 4
- HIGH: 6
- MEDIUM: 3
- LOW: 1

### Project-Specific Insight
rite plugin の Wiki 機能は LLM が bash block 内で会話境界を跨いで state を伝達する (cross-boundary state transfer pattern) 性質上、placeholder / emit / parse の 3 点同期が drift 検出なしで silent regression を生む。本 PR で `bash-cross-boundary-state-transfer.md` という canonical reference を新設したのは良い方向だが、既存実装との整合性確認 (Phase 6.2 の参照実装登録、対称 trap 設置) を同時に行う規律が必要。

