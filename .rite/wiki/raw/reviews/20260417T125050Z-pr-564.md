---
type: reviews
source_ref: "pr-564"
captured_at: "2026-04-17T12:50:50+00:00"
pr_number: 564
title: "PR #564 review results"
ingested: true
ingest_status: skipped
skip_reason: "cycle 4 review: 対称化微調整、既存 asymmetric-fix-transcription.md でカバー済"
---

## Review Results

- **PR**: #564 — fix(wiki): lint missing を 2 カテゴリに分離し .gitignore で develop 漏洩を防ぐ
- **Type**: review
- **Reviewed at**: 2026-04-17T21:49:11+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality

### Finding Patterns

本 PR は indicator refactor + 最終防御線追加 (Defense-in-Depth) の典型例。主要な review observations:

1. **6 フィールド契約の 3 箇所同期検証パターンが機能した**: ingest.md Phase 8.3 の parse regex (`^Lint: contradictions=([0-9]+), stale=([0-9]+), orphans=([0-9]+), missing_concept=([0-9]+), unregistered_raw=([0-9]+), broken_refs=([0-9]+)$`) と lint.md Phase 9.2 の stdout フォーマット、設計文書 (experience-heuristics-persistence-layer.md L69) の F4 Lint サイクル表が文字単位で同期。類似の「契約同期」型 refactor では、AC anchor / prose / コード emit 順 の 3 箇所同期検証が有効 (drift 検出 lint の補完として実用)。
2. **`log_read_ok` 4 値 enum の stdout 伝達契約**: Bash tool 呼び出し境界でシェル変数は保持されないため、`echo "log_read_ok=$log_read_ok"` で LLM が会話コンテキストで読み取る契約。`---skipped_refs_begin---` / `---skipped_refs_end---` delimiter marker も 0 件時必ず出力される positive confirmation パターンが優秀。同一 pattern は他コマンドで再利用可能な設計資産。
3. **Cateorical separation の設計原則**: 元々単一 `missing` カテゴリに「真の欠落」と「skip 済み raw」が混在して health metric が歪んで見えていた → 2 カテゴリ分離 (`missing_concept` は `n_warnings` 加算 / `unregistered_raw` は informational 不加算) で誠実な health reporting を実現。類似の「informational なのに warning としてカウントされる」antipattern が他箇所にあれば同じ治療が適用可能。
4. **`.gitignore` による silent leak 最終防御線**: `.rite/wiki/` を明示除外することで `wiki-ingest-commit.sh` の stash/cleanup が silent skip した場合でも develop に raw source が漏れないよう最終防御線を敷設。Defense-in-Depth の典型例。
5. **docstring / negation コメント の精度**: `same_branch` 戦略向けに negation 手順 (`!.rite/wiki/` + `!.rite/wiki/**` の 2 行) を正しく提示し、動作確認コマンド `git check-ignore -v` も明示。gitignore 仕様の「ignored dir 配下 un-ignore には /dir と /dir/** 双方必要」に整合。
6. **i18n parity**: README.md / README.ja.md、SPEC.md / SPEC.ja.md で片側 drift なし (6 カテゴリ列挙順も完全一致)。ja と en の同時更新で両方とも新 6 フィールド形式に同期。

### Severity Distribution

- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW: 0

### Recommendations (別 Issue 候補)

- Phase 2.2 cleanup 関数の空引数ガード backport (Phase 6.0 と対称性維持)
- Phase 2.2 と Phase 6.0 cleanup 関数の shared lib 抽出 (「兄弟 shell script の重複 helper は shared lib 抽出で解く」経験則適用)
- L-09 対応 sub-path WARNING の separate_branch / same_branch 文言共通化
- docs/CONFIGURATION.md の `branch_strategy: same_branch` 事例ブロックから `.gitignore` の negation comment への相互参照追加 (silent leak の UX 改善)
- docs/SPEC.md の `broken refs` と他箇所の `broken cross-refs` の用語微妙ドリフト整理
- `log_read_ok` 4 値 enum pattern を references/ 配下の pattern doc として抽出

