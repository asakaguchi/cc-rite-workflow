---
type: reviews
source_ref: "pr-1246"
captured_at: "2026-06-02T06:47:58+00:00"
pr_number: 1246
title: "PR #1246 review results"
ingested: true
---

## Review Results

- **PR**: #1246 — fix(hooks): settings-local-rite-hook-cleanup の mv 失敗経路に stderr WARNING を追加
- **Type**: review
- **Reviewed at**: 2026-06-02T15:47:03+09:00
- **Reviewers**: error-handling, code-quality, test, prompt-engineer, security

### Finding Patterns
- **`A && B` 短絡が関数/スクリプト末尾文になると exit code が leak する** (HIGH, current-pr): bash で `[ -n "$var" ] && cmd` が script の最終実行文のとき、`$var` が空だと `[ -n "" ]` が rc=1 を返し && 短絡で compound が 1 を返す。末尾に `exit 0` がないと、非ブロッキング契約 (exit 0 always) を documented する helper でも script 全体が exit 1 を leak する。本 PR では mv stderr 退避用の `mv_err=$(mktemp)` が disk pressure で失敗 (`mv_err=""`) した CLEANED 経路で顕現。error-handling reviewer が runtime observation (mktemp-shim で no-arg call のみ失敗させる) で検出、code-quality/security は normal path のみテストして見落とし → cross-validation で coverage gap を解消。
- **error-surfacing 追加時の exit-code 契約後退**: silent skip を WARNING surfacing に変える PR で、新規追加した cleanup 行 (`[ -n "$mv_err" ] && rm`) が末尾文になり、それまで `echo` (常に rc=0) で守られていた暗黙 exit 0 契約を崩した。先例 `issue-comment-wm-sync.sh:99-104` の error-surfacing pattern 自体は健全だが、末尾 `exit 0` 明示の sibling 規約に未準拠だった。
- **テストの exit-code 契約 pin 不足**: S-6 (mv 失敗) は token/exit 0/WARNING を pin するが、CLEANED 経路 + 内側 mktemp 失敗の exit 0 を pin するケースがなく、本 HIGH を捕捉できなかった。`assert_grep 'WARNING.*mv failed'` は rc 値を検証しない。
- **検証の責務分離**: code-quality/security の「exit 0 維持」評価は彼らがテストした path (normal CLEANED / mv-failure) では正しく、特定 edge case (CLEANED + 内側 mktemp 失敗) 未テストの coverage gap。複数 reviewer の評価が割れたとき、より具体的な runtime evidence を持つ finding を実機再現で確証するのが正しい解決。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 0
- LOW: 0

