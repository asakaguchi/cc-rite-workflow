---
type: fixes
source_ref: "pr-608"
captured_at: "2026-04-19T19:50:08+00:00"
pr_number: 608
title: "PR #608 fix results"
ingested: true
ingest_status: skipped
skip_reason: "ポリシー drift 同期修正 / step 粒度対称化 / whitelist 誤依存 prose / convention 非対称意図化パターンは既存ページ (asymmetric-fix-transcription / prompt-numbered-list-isomorphic-structure / prose-design-without-backing-implementation / state-machine-dual-location-sync) で網羅済み (raw: pr-608)"
---

## Fix Results

- **PR**: #608 — fix(pr-cleanup): wiki-auto-ingest 後の Phase 5 停止を多層防御で解消
- **Type**: fix
- **Fixed at**: 2026-04-20T02:15:00+09:00

### Fix Patterns
PR #608 レビューで検出された 10 findings (1 HIGH + 4 MEDIUM + 5 LOW) に対応。主要パターン:

1. **ポリシー drift の同期修正 (HIGH)**: create.md の多層防御パターンを移植する際、設計判断を「常に出力」に flip したが上位セクション (L105 / L852 / Output ordering) の旧 conditional 記述が未同期で残存。2 reviewer が独立同定 (High Confidence)。修正では 4 箇所 (L105 / L852 / L1023 / L1028/L1032) を「Phase 9.1 設計判断により実行経路を問わず常に出力」に統一し、single source of truth を Phase 9.1 に集約。
2. **step 粒度の対称化 (MEDIUM/LOW)**: cleanup.md Phase 5.3 Output ordering (4 step) と本体 Step 1/2 (2 step) の番号対応が暗黙化、Correct-pattern Step 2 で Phase 5.1/5.2 を 1 line collapse。create.md の terminal sub-skill 1 step 構成と粒度乖離。修正では「Phase 5.1/5.2 は前段」「Phase 5.3 Step 1/2」の明示対応付けを prose 化。
3. **whitelist 保護への誤った依存 prose (MEDIUM)**: cleanup.md で「retrograde transition 発生時に whitelist が block する」と示唆する prose が、実際の terminal acceptance (next のみ判定、prev が terminal でも accept) と矛盾。修正では「Step 1 実行自体を skip することで保護」という whitelist 非依存の表現に書き換え。
4. **hook コメント / glob の drift / 非対称 (MEDIUM/LOW)**: phase-transition-whitelist.sh:153 "Phase 0" vs actual "Phase 1.0"、session-end.sh `cleanup*` glob vs `create_*`、test fresh_ts の cross-TC 依存。いずれも機械検証可能な書換えで解消。
5. **convention 非対称の意図化 (LOW)**: create.md と cleanup.md で flow-state-update.sh 呼び出しパターンが乖離しているが、cleanup 側は stop-guard 保護欠落時の UX 影響が大きいため defense-in-depth を先行採用した意図的乖離。設計 note として明示し、convention 統一は別 Issue で追跡。

### Statistics
- Total findings: 10
- Fixed: 10 (prose / comment / glob / test 独立再代入)
- Replied: 0
- Skipped (separate Issue): 0

### 経験則
- prose 仕様の policy flip 時は **関連セクション全箇所の同期更新**を grep で機械検証すべき (drift 防止)。特に「シンプルさ優先で常時出力」のような設計判断は上位セクション (Completion marker convention / Step 内 prose) まで波及する
- hook enforcement の行政コメントと actual heading の drift は `grep -F '{section name}'` で検証可能。Phase 番号や section heading の変更時は cross-file grep を実行する
- test の cross-TC 依存 (fresh_ts 等の test-local 変数継承) は各 TC 冒頭で `var="${var:-default}"` パターンを独立適用することで排除可能
- review-fix 時の修正は `AC-6 contract phrase 件数` + `既存テスト通過率 (baseline との差分)` を機械検証することで silent regression リスクを最小化できる

