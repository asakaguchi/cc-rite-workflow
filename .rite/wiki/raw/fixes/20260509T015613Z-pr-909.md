---
type: fixes
source_ref: "pr-909"
captured_at: "2026-05-09T01:56:13+00:00"
pr_number: 909
title: "PR #909 fix results (cycle 2)"
ingested: false
---

## Fix Results

- **PR**: #909
- **Type**: fix
- **Fixed at**: 2026-05-09T10:55:00+09:00

### Fix Patterns

cycle 2 で code-quality-reviewer から MEDIUM 1 + LOW 2 を受け、3 件すべてに対応:

1. **F-01 (MEDIUM) — 同一ファイル内の dead reference**: ファイル冒頭の Assertions 一覧 (line 17) が旧仕様 `Mandatory After ≥ 30` のままで、実装 (line 102-106) の `≥ 17` heading-anchor 限定と乖離していた。修正で line 17 を `Mandatory After heading-anchor (h3+h4) ≥ 17 (実測: h3 14 + h4 3)` に同期更新。Cross-File Impact (Wiki PR #848 の Wording-revision drift / pin-claim factual error) の同一ファイル版として解消。

2. **F-02 (LOW) — 副次的主張のファクト誤認**: コメントの「`After [A-Za-z]` で `### 🚨 After-Review` (hyphen) の取りこぼしも防ぐ」記述が事実誤認。`After [A-Za-z]` は literal 空白を要求するためハイフン形にはマッチしない (Bash で `echo "### 🚨 After-Review" | grep -oE '...After [A-Za-z]' → NO MATCH` で実証)。副次的主張を削除し「必要時に `After[ -][A-Za-z]` 等への拡張を検討」と書き換え。

3. **F-03 (LOW) — 暗黙メンテナンスルールの明文化**: 閾値 17 と内訳 (h3 14 + h4 3) が複数箇所に分散しており、後続 PR で heading 追加時の同期更新ルールが暗黙的だった。コメント末尾に「heading 追加/削除時は内訳 (h3 N / h4 M / 合計 K) と閾値 `-ge K` / `>=K` を同期更新」のルールを 1 行追記。

### Lessons (経験則化候補)

- **Wording-revision drift の伝播**: 実装変更時に冒頭仕様サマリも同期更新する。dead reference は将来の reviewer に「どちらが正解か」を判断不能にする liability になる。
- **regex 副次的主張のファクト検証**: 「将来的な X 取りこぼし防止」という副次的主張は POSIX ERE の literal 動作で実証検証する。検証なしの主張は将来「守れているはず」誤前提を生む。
- **更新ルールの明文化**: 数値閾値 + 内訳 (h3 N / h4 M / 合計 K) の同期更新ルールはコメント末尾 1 行で明示する。暗黙ルールは drift 要因。

### Statistics
- Total findings: 3
- Fixed: 3
- Replied: 0
- Skipped (separate Issue): 0

