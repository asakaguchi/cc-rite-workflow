---
type: fixes
source_ref: "pr-747"
captured_at: "2026-04-30T03:22:40+00:00"
pr_number: 747
title: "PR #747 fix cycle 5"
ingested: true
ingest_status: skipped
skip_reason: "PR #747 cycle 5 fix (header doc rot / \\${RANDOM:-0} 防御 / canonical helper \"always exit 0\" 信頼回避) — 個別 bash defensive pattern で独立経験則化価値低、既存 mktemp-failure-surface-warning / drift-check-anchor-semantic-name の応用範疇"
---

## Fix Results

- **PR**: #747
- **Cycle**: 5 (累計 21 findings: 3 + 6 + 2 + 7 + 3)
- **Findings addressed**: 3 (3 LOW)

### Fix Patterns
- **header doc Comment Rot 防止**: 実装変更 (BACKUP_FILE suffix 拡張) に対してファイル冒頭の summary doc が追従しない rot は cycle reviewer が必ず検出する。実装変更時は header doc を同時更新する習慣が必要。
- **`${RANDOM:-0}` 防御パターン**: 親 shell が `unset RANDOM` した場合の空展開回避。bash subprocess では magic 変数として復活するため通常経路では trigger しないが、defense-in-depth として fallback を入れる。
- **canonical helper の "always exit 0" contract に依存しない soft fallback**: helper の API contract を信頼しすぎると、将来 contract regression で caller 全体が abort する fragile pattern を作る。`|| empty=""` で fallback を入れることで helper failure を soft degradation に変換できる (silent failure ではなく WARNING は helper 自身が emit する前提)。

### Statistics
- Total findings: 3
- Fixed: 3

