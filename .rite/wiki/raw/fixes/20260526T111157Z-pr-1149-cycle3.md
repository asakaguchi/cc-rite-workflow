---
type: fixes
source_ref: "pr-1149-cycle3"
captured_at: "2026-05-26T11:11:57+00:00"
pr_number: 1149
title: "PR #1149 fix results (cycle 3)"
ingested: false
---

## Fix Results

- **PR**: #1149
- **Type**: fix (cycle 3 — cycle 2 regression 全件対応)
- **Fixed at**: 2026-05-26T20:15:00+09:00

### Fix Patterns

- **precedent-aware-rewrite-rule**: cycle 1 fix の F-06 inline rewrite で peer file convention を見逃したため、cycle 2 で 4 件の新規 regression を導入した。本 cycle ではこの教訓を反映し、`commands/issue/create.md` の 3 sites (L157 / L287 / L348) を直接 grep + Read してから skeleton を再構築した。教訓を pattern 化: 「**大規模 refactor の inline rewrite 時は peer file convention を必ず参照すること**」。同じ機能を実装する peer file が存在する場合、その convention (single-quoted HEREDOC / hardcode 値 / fail-safe pattern / placeholder 定義) をテンプレートとして再構築するのが robust。
- **gh-issue-create-label-precreate-required**: `gh issue create --label X` は X 未存在時に `could not add label: 'X' not found` で fail し Issue creation 自体が失敗する (runtime verify 済)。`gh label create X --color ... 2>/dev/null || true` を冪等事前作成 step として **必須** にする (`|| true` で既存 label のエラーを無視)。`create-issue-with-projects.sh` 等の helper script に label 事前作成 logic を含めるか、caller 側で前置することのいずれか必須。
- **historical-doc-preamble-over-renumber**: 既に閉じた anti-pattern doc / design doc / test spec の rewriting (旧 Phase X.Y を新表記に置換) は不要。これらは「歴史的記述」として保持するのが正しい — caller (cleanup.md / review.md 等) の renumber が完了した後に、callee 側の historical doc に「Status: 構造的に解消済 (Issue #N / PR #M)」preamble を追加するだけで読み手に混乱を起こさない。asymmetric-fix-transcription 回避の正攻法。
- **changelog-i18n-sync-on-file-deletion**: file 削除時は CHANGELOG (英語版 + 日本語版 i18n parity) からの削除 + 削除事実の新規エントリ記載が必須。両方が stale な状態を「i18n parity 維持」と誤認しない (両方とも嘘になる)。
- **layer-retire-table-and-prose-sync**: Layer 1/2/3/... 等の defense-in-depth 層を retire する際、table 行のみの retire マーキングでは不十分で、同一 § 内の prose 4+ 箇所で「primary active layers」「active enforcement」等として記述された参照を同時に更新する必要がある。grep で同 § 内全箇所を網羅すること。

### Statistics

- Total findings: 10
- Fixed: 10 (cycle 1 introduced regression が 7 件、pre-existing が 3 件)
- Cycle: 3
- Tests: 43/43 pass
- 累積 file 変更: 25 files (cycle 1: 16 + cycle 2: 0 retain + cycle 3: 10、一部重複あり)

### 解消した meta-pattern

cycle 1 で導入した本 PR 自身による fix-introduces-regressions-on-inline-rewrite を cycle 3 で **peer file convention 参照** という構造的解で解消。pattern として ingest 済 (precedent-aware-rewrite-rule)。

