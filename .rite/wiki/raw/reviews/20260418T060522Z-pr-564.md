---
type: reviews
source_ref: "pr-564"
captured_at: "2026-04-18T06:05:22+00:00"
pr_number: 564
title: "PR #564 review results"
ingested: true
ingest_status: skipped
skip_reason: "cycle 10 review: commit message drift-check anchor 追加、既存 drift-check-anchor-prose-code-sync.md でカバー済"
---

## Review Results

- **PR**: #564 — fix(wiki): lint missing を 2 カテゴリに分離し .gitignore で develop 漏洩を防ぐ
- **Type**: review
- **Reviewed at**: 2026-04-18T15:02:14+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling

### Finding Patterns

主要な指摘パターン:

1. **3 行 loud emit 規範の対称性違反**: 同 PR 内で大半の mktemp 失敗 WARNING を 3 行 (WARNING + 対処 + 影響) に揃えているが、一部 site (Phase 6.0 awk_sort_err / ingest.md Phase 5.2 add_err) に 1 行 fallback が残存。Pattern 3 規範文書 (`bash-cross-boundary-state-transfer.md`) を新規追加する PR では、その規範を全 site で対称的に適用する必要がある。

2. **canonical reference サンプル内の silent failure**: 新規追加した `bash-cross-boundary-state-transfer.md` Pattern 3 example で `else rc=$?` を欠落させ未定義 `$rc` を参照する模範例を残してしまった。reference 文書はコピペ利用される前提のため、サンプルコード自体に silent failure pattern を含めると下流に regression を伝播させる。本文の他 Phase の正典実装と一字一句揃える必要がある。

3. **case 文インデント非対称**: 同一 case 内でアームごとに 0/2/4 space の異なる indent style が混在 (Phase 8.3)。bash 自体は indent に依存しないが、80+ 行の長い case body では reader が「`*)` は別ブロック?」と誤認するリスク。case 化リファクタ時は同 PR 内の他 case site (Phase 6.0/6.2/8.2) と同じ indent rule を適用する。

4. **YAML frontmatter description vs 本文階層 drift**: 本文側で「5 ブロッキング + 1 informational」の階層構造に変更しても、YAML frontmatter description / SKILL.md description は flat 列挙のまま残存しがち。description は Skill 一覧で最初に表示される文字列なので、ユーザの期待値ずれを生む。

5. **prompt 内の動作 bullet 欠落**: 同型構造の numbered list で一部 step だけ動作詳細 bullet が抜けるケース (ingest.md Phase 8.3 step 1)。LLM は numbered prose を読んで実装する想定なので、step 1 だけ動作が不明瞭になる。同型 step 2/3/4 と対称化必須。

6. **stale 行番号参照**: コメント内に `下記 905 の grep regex` のような行番号参照が残ると、リファクタ後 drift する。同 PR 内の他コメントは「Phase 番号 + semantic 名」の参照原則を明示しているため、原則逸脱は drift の温床。

7. **参照リスト更新漏れ**: 新規追加した cleanup function (Phase 8.3) が reference 文書 (`bash-trap-patterns.md`) の bullet list に追加されていない。canonical reference を整備する際は同 PR で追加された全 site を同 PR 内で参照に登録する。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 6
- LOW: 1

### キーパターン
- 「対称性契約」(同 PR 内の 5 site fail-fast / 3 site placeholder gate / 4 行 trap 等) を打ち出す PR では、契約準拠を全 site で機械検証可能な形 (drift-check lint) で固定化することが望ましい
- 新規 reference 文書 (canonical) のサンプルコードは既存実装と同一 rigour で書く必要がある

