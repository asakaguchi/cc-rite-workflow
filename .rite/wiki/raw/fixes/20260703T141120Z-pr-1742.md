---
type: fixes
source_ref: "pr-1742"
captured_at: "2026-07-03T14:11:20+00:00"
pr_number: 1742
title: "PR #1742 fix results"
ingested: true
---

## Fix Results

- **PR**: #1742
- **Type**: fix
- **Fixed at**: 2026-07-03

### Fix Patterns
- プラットフォーム依存の保守的縮退経路（start-token 取得不能環境での legacy PID-only hold）を持つ実装をテストするとき、成功アサートするテスト（TC-014）と skip ガードを持つテスト（TC-015）を左右対称に揃える。片方だけガードが欠けると「コードは正しいがテストが特定環境で偽 FAIL」する非対称バグになる。
- flock 有り/無しの 2 経路を持つ bash 実装で no-flock 経路を決定的にテストするには、flock を除いた PATH スタブ（needed cmd を symlink、flock だけ除外）で本体を起動する。ただし no-flock の並行 CAS は best-effort 設計のため exactly-one はアサートしない（steal-success のみ検証）。PATH スタブは host 固有の tool path を取りこぼす可能性があるため、sanity probe で失敗時は fail ではなく skip に倒す。
- SPEC の機構要約は over-claim を避け、実装の保守的縮退経路（legacy / プラットフォーム非対応）を「when a start-token was recorded」等の条件付き表現で明記する。

### Statistics
- Total findings: 3
- Fixed: 3
- Replied: 0

