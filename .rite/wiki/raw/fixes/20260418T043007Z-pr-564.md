---
type: fixes
source_ref: "pr-564"
captured_at: "2026-04-18T04:30:07+00:00"
pr_number: 564
title: "PR #564 fix results (9th cycle MEDIUM 7 / LOW 3)"
ingested: true
ingest_status: skipped
skip_reason: "cycle 9 fix: placeholder-residue gate 対称化、drift-check-anchor-prose-code-sync.md でカバー済"
---

## Fix Results

- **PR**: #564
- **Type**: fix
- **Fixed at**: 2026-04-18

### Fix Patterns

1. **verification-first ドキュメント再構成** (F-02 / F-05 / F-06 / F-07 / F-08 / F-10 — 6 件まとめ):
   sanity check を「何を実行すれば negation が効いていると判定できるか」という verification 手順で書き直し、検証コマンド (`git add --dry-run` + probe file の mkdir+touch) を Step 化した。`git check-ignore -v` の負例挙動 (negation 下で rc=0/rc=1 双方ありえる) を正確に説明し、確実性の低い check を正典から降格。
2. **scope prefix 命名規約への強制統一** (F-01): `_rite_ingest_phase52_cleanup` のような scope prefix 欠落名を `_rite_wiki_ingest_phase52_cleanup` に rename し、bash-trap-patterns.md 参照実装一覧にも追記。scope 衝突予防として恒久化。
3. **fragile parsing contract の強化** (F-03): stdout の「1 行目」固定 parse → 全行 scan + 最初 match 採用に変更。downstream (lint.md) 側の intermediate echo 混入に対する defense-in-depth。contract の変更なしで ingest 側の堅牢化だけで達成。
4. **reference doc と canonical 実装の対称化** (F-04): reference ドキュメント内の WARNING example placeholder を、実装 (lint.md Phase 6.0) の canonical 4 行構造 (WARNING + head -3 surface + impact + 対処) に揃えた。observability 対称性を reference で学習できるようにする。
5. **grep anchor による drift risk の排除** (F-09): 他文書 (CONFIGURATION.md) から `.gitignore` への参照を「行番号 / 曖昧な位置指示」ではなく `# >>> gitignore-wiki-section-start` のような grep 可能 anchor marker 経由にする。範囲が移動しても参照が壊れない。

### 過剰反応の回避

- F-01 対応で「旧名は互換のため両方保持」のような backwards-compat hack は行わず、完全リネームを実施 (scope prefix 規約違反の silent 継続を防ぐ)。
- F-03 対応で lint.md 側の Phase 9.2 契約を強化する「intermediate echo は /dev/null/stderr に振り分ける」選択肢は採用せず、ingest 側の parse を堅牢化する片側実装にとどめた (契約変更は影響範囲が広いため)。両側実装は不要。

### Statistics
- Total findings: 10
- Fixed: 10
- Replied: 0
- Skipped (separate Issue): 0

