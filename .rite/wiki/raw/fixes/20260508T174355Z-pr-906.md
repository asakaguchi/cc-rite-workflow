---
type: fixes
source_ref: "pr-906"
captured_at: "2026-05-08T17:43:55+00:00"
pr_number: 906
title: "PR #906 fix results (cycle 3)"
ingested: false
---

## Fix Results (cycle 3)

- **PR**: #906
- **Type**: fix
- **Cycle**: 3

### Critical Bug Discovery

cycle 3 review で **CRITICAL pipefail bug** を検出。`set -euo pipefail` 配下で `grep -oE 'pattern' file | wc -l | tr -d ' '` は grep 0 マッチ (exit 1) で pipeline 全体を abort。後続 PR で `🚨` / `cycle N` / `Issue #N` が ratchet ideal 値 (= 0 件) に到達した瞬間に test が silent abort し pass/fail のいずれも emit されない致命的経路。実機で空 file に対する `grep -oE` で再現確認 (exit 1)。

修正: 5 件すべての pipeline を `{ grep ... || true; } | wc -l | tr -d ' '` 形式に変更。

### Cycle Degeneration Pattern

cycle 1: 下限 assert を `grep -c` (line) → `grep -oE | wc -l` (occurrence) に変更、ただし `|| true` 防御を失った
cycle 2: 上限 🚨 を同パターンに変更、同じく `|| true` を持たない (修正スコープ漏れの完了でしたが pipefail 防御は未遂)
cycle 3: pipefail bug が CRITICAL として顕在化、5 件すべて `{ grep || true; } | wc -l` に修正

これは **同一 fingerprint (occurrence vs line / pipefail 防御) が 3 cycle に渡って degenerate した経緯** で、Quality Signal 1 (fingerprint cycling) の典型例。

### F-04 Reverse Direction

cycle 2 で `print_summary "$(basename "$0" .test.sh)"` に変更したが、cycle 3 reviewer が「sibling 4 test は `$(basename "$0")` 慣習」と指摘。実機 grep で確認したところ cycle 3 reviewer の主張が正しく、cycle 2 修正は逆方向だった (cycle 2 reviewer の advice 誤り)。revert で対応。

### Latent Edge Cases (Issue #908 化)

cycle 3 で検出された 3 件 LOW (indented bash block / shell comment match / total=0 ガード) は現状 start.md に該当パターンなしの latent risk のため別 Issue 化。

### Statistics
- Cycle 3 findings: 6 (CRITICAL 1, LOW 5)
- Fixed: 3 (CRITICAL pipefail + F-04 revert + docstring 注記)
- Separate Issue: 1 (Issue #908 with 3 latent LOW)
- Total fix cycles for PR #906: 3

