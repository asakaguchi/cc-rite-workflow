---
type: fixes
source_ref: "pr-1310"
captured_at: "2026-06-08T14:50:54+00:00"
pr_number: 1310
title: "PR #1310 fix results"
ingested: false
---

## Fix Results

- **PR**: #1310
- **Type**: fix
- **Reviewers addressed**: code-quality, error-handling, test, prompt-engineer

### Fix Patterns
inline → delegate refactor の dominant failure: 3 段プロトコル分割時に旧 `trap ... EXIT` cleanup 契約を落とす退行 (HIGH、2 reviewer 独立検出)。修正方針は (C) block に signal-specific trap を再設置し inline rm -rf を撤去、workdir を literal placeholder ではなく shell 変数 `pr_workdir` に束縛して `[ -d ]` guard 付き cleanup にする (placeholder 置換漏れ時の rm -rf 誤動作も同時に防ぐ)。新規 lint signal 追加時の頻出ギャップ: (a) 検出 anchor (`create` 限定) を守る負例 TC 欠落、(b) canonical な複数行 (backslash 行継続) form の取りこぼし、(c) 空 literal (`--title ""`) 誤検知。awk 検出は block-level 継続トラッキング (`gh_create_active` を `\` 終端行で維持) で複数行対応し、bracket expression を `[^$]`→`[^$"']` に拡張して空 title と `$var` を同時に除外。body/title の error guard は対称化が原則 (片方だけ `[ ! -s ]` チェックがあると非対称な silent failure 経路になる)。

### Statistics
- Total findings: 6
- Fixed: 5
- Documented as follow-up (Cause A trade-off): 1

