---
type: reviews
source_ref: "pr-1319"
captured_at: "2026-06-09T08:52:10+00:00"
pr_number: 1319
title: "PR #1319 review results"
ingested: true
---

## Review Results

- **PR**: #1319 — test(scripts): review-source-resolve.test.sh の未カバー error 経路に assertion を追加
- **Type**: review
- **Reviewed at**: 2026-06-09T17:50:49+09:00
- **Reviewers**: test, performance, error-handling, security

### Finding Patterns

0 findings / 4 reviewers / 1 cycle mergeable。テスト専用 PR (production script `review-source-resolve.sh` 不変、test ファイルに +125/-0、37→73 assertions)。

レビューの核心は「test が主張する経路を実際に exercise する non-vacuous なものか」「assert する reason/marker 文字列が実装の実際の emit と完全一致するか」の 2 点に集約された:

- test-reviewer / error-handling-reviewer ともに新規 9 件の `assert_err_has "reason=..."` を実装 `review-source-resolve.sh` の `echo "[CONTEXT] ..."` 行と 1 件ずつ突合し全一致を確認。reason 文字列の乖離があれば test は無意味になるため、ここが test の正しさの核心。
- negative assertion `assert_err_lacks "REVIEW_SOURCE_STALE=1"` の non-vacuity は、Test 6/8 が同一 emitter に対し match(STALE 不在)/mismatch(STALE 存在) の 2 ケースを走らせる **differential 構造**で担保される (mismatch ケースが「STALE は本経路で emit され得る」ことを実証するため match ケースの negative assertion が trivially-passing でない)。
- match/mismatch の決定性は sandbox 実 HEAD (`git rev-parse HEAD`) と all-zeros `BOGUS_SHA` (Git 予約 null-object sentinel、実 commit hash には成り得ない) の組で保証。
- `set -euo pipefail` が `HEAD_SHA=$(...)` 失敗時に top-level コマンド置換代入を abort するため、空 HEAD_SHA による silent regression (壊れた実装でも green) 経路は閉じている。
- corrupt-rename Instance 2/2 (`schema_required_fields_missing` + `.corrupt-*` rename) が Test 9 で追加され、実装コメント「Instance 1/2 と複製、変更時は両方更新」の test 側 drift-prevention 担保が完成。
- security: サンドボックス隔離 (実装は相対 `.rite/review-results`、test は `run()` 内 `cd "$SANDBOX"` で `/tmp` 配下に隔離) を実リポジトリ root を cwd にして実機検証 (`.rite` find hash 前後不変、`.corrupt-*` leak なし、`git status --porcelain` 不変) で確認。破壊的 `mv` 副作用も sandbox 内に限定。

全 reviewer が Finding Quality Guardrail / Observed Likelihood Gate / revert test を適用し bikeshedding/hypothetical/style-only を filter した結果 0 findings。Issue #1220 の「8 fatal 経路」と本 PR「7 件追加」の差は、到達不能 defensive guard (Priority 3 が常に pr_comment を set するため到達しない post-chain guard 2 件) と環境依存 path (bash 3.x 限定の `bash_version_incompatible`) の妥当な除外で説明可能。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW: 0

