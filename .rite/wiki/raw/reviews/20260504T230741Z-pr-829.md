---
type: reviews
source_ref: "pr-829"
captured_at: "2026-05-04T23:07:41+00:00"
pr_number: 829
title: "PR #829 review results"
ingested: true
ingest_status: skipped
skip_reason: "PR #829 review raw (overview only): 同 PR の fix raws (cycle 1 / cycle 2) と内容重複のため skip。経験則は fix raws 経由で 2 既存ページに追記済み"
---

## Review Results

- **PR**: #829 — docs(rite): /rite:issue:create ゼロベース再設計 plan (Phase E)
- **Type**: review
- **Reviewed at**: 2026-05-05T08:05:44+09:00
- **Reviewers**: tech-writer, code-quality

### Finding Patterns

design doc PR で頻出する 3 パターンを実測:

1. **数値主張 vs 実測値の drift (CRITICAL × 2)**: design doc Section 2 の「実測結果」(Phase 番号列挙、サブセクション総計) が grep 実測と乖離。design doc 作成時に grep で実測値を機械生成する step が plan に組み込まれていなかった。今後の plan PR では `scripts/measure-*.sh` 先行 PR を option として明記すべき。
2. **存在しないファイルへの参照 (CRITICAL × 2)**: `regression-history.md` (架空) と `run-all.sh` (実際は `run-tests.sh`) の 2 件。tech-writer の Cross-Reference verification + code-quality の find/ls 検証で双方検出。design doc 内の実コード reference は `find` / `ls` で実在 verify する step が plan creation の暗黙 contract として必要。
3. **同一 PR 内 sibling 表の Asymmetric drift (HIGH × 2)**: Phase 0.4.x 統合範囲が 4 sibling 表 (Section 3 / 4.1 / 6.2 / 8.1) で 2 件 0.4.2 含む / 2 件欠落、Section 3.1 の references 列挙が「7 ファイル」と claim するが 6 件のみ列挙。Wiki 経験則「Asymmetric Fix Transcription」が plan layer (実コード以外) でも発火する事例。

### Severity Distribution

- CRITICAL: 4 (Phase 番号列挙の事実誤認 / run-all.sh 不在 / regression-history.md 不在 / awk syntax error)
- HIGH: 5 (3-site/4-site 不整合 / gh api invalid verification / 0.4.2 asymmetric / sub-skill-handoff-contract 列挙漏れ / PR-E1 grep trivial pass)
- MEDIUM: 2 (NFR-6 違反先送り / Section ID タクソノミー不一致)
- LOW: 2 (preset baseline 不整合 / 集計範囲不整合)

### Reviewer cross-validation

両 reviewer が独立に検出した重複 finding 2 件:
- `regression-history.md` 不在 (両者一致 → confidence boost)
- awk syntax error (両者一致 → confidence boost)

